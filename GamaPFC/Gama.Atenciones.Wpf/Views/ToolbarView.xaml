<UserControl x:Class="Gama.Atenciones.Wpf.Views.ToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gama.Atenciones.Wpf.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" d:DesignHeight="76.452" d:DesignWidth="377.055">
  <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
    <Button Background="Transparent" BorderThickness="0"
            Command="{Binding NuevaPersonaCommand}">
      <StackPanel>
        <Image Source="{StaticResource IconoNuevaPersona}" Height="{StaticResource NavigationIconSize}"/>
        <!--<TextBlock MaxWidth="60" TextWrapping="Wrap" TextAlignment="Center"
                   FontWeight="Light">
          Nueva Persona
        </TextBlock>-->
      </StackPanel>
    </Button>
    <Button Background="Transparent" BorderThickness="0"
            Command="{Binding ExportarCommand}">
      <StackPanel>
        <Image Source="{StaticResource IconoExportar}" Height="{StaticResource NavigationIconSize}"/>
      </StackPanel>
    </Button>
  </StackPanel>
</UserControl>
