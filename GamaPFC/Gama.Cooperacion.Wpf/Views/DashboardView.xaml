<UserControl x:Class="Gama.Cooperacion.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gama.Cooperacion.Wpf.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="454.754">
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Green.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto" MinHeight="200" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition />
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

    <GroupBox Header="Últimas Actividades" Grid.Row="1">
      <ListBox ItemsSource="{Binding UltimasActividades}"
               DisplayMemberPath="Titulo"/>
    </GroupBox>
    <GroupBox Header="Últimos Cooperantes" Grid.Column="1" Grid.Row="1">
      <ListBox ItemsSource="{Binding UltimosCooperantes}"
               DisplayMemberPath="Nombre"/>
    </GroupBox>
    <GroupBox Header="Últimas Incidencias" Grid.Column="2" Grid.Row="1">
      <ListBox ItemsSource="{Binding UltimasIncidencias}"
               DisplayMemberPath="Contenido"/>
    </GroupBox>
  </Grid>
</UserControl>
